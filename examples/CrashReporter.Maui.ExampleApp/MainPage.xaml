<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CrashReporter.Maui.ExampleApp.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="32,0"
            Spacing="24">

            <Button
                x:Name="CounterBtn"
                Text="Increment counter" 
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="CustomCrashBtn"
                Text="Custom crash" 
                Clicked="OnCustomCrashClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="NativeCrashBtn"
                Text="Native crash" 
                Clicked="OnNativeCrashClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="CheckForCrashesBtn"
                Text="Check for crashes" 
                Clicked="OnCheckForCrashesClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
